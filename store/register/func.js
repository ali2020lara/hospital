const isLogin = (username)=> {
    if (username === 'reza' || username === 'ali') {
        return true;
    } else {
        return false;
    }
}
const register = (username)=> {
    if (username === 'reza' || username === 'ali') {
        return true;
    } else {
        return false;
    }
}
const own = (username)=> {
    if (username === 'reza' || username === 'ali') {
        return true;
    } else {
        return false;
    }
}